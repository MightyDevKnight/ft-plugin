import { extractArgTypes } from '@pxtrn/storybook-addon-docs-stencil';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html, render } from 'lit-html';

<Meta title="Components/Actions/Link" component="sux-link" argTypes={extractArgTypes('sux-link')} />

# Actions / Link

Body is a typography component used to create various levels of hierarchies between text.

## Table of options / API

From the design point of view, each component has a number of options. These options and their names are platform agnostic, and each implementation should adapt these to fit into their framework.

<ArgsTable of="sux-link" />

### Classification

Headings can be displayed in serif or sans serif (default).

## Variants

### CTA

export const Default = args => {
  return html` <div style="padding: 10%; display: flex; justify-content: center; gap:16px;"></div> `;
};

<Canvas>
  <Story name="Playground">{Default.bind()}</Story>
</Canvas>
